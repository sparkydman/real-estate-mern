> create a property
	>> title
	>> location
	>> address
	>> value_for([sale,rent])
	>> price
	>> agent({user})
	>> status([new,reconstructed,old,natural])
	>> category([duplex,bowngalow,flat,self-contain,room,attach,container,shop,warehouse,land])
	>> compound_space
	>> property_size
	>> quantity
	>> imges
	>> description
	>> slug
	>> keywords(string array)
	>> createdAt